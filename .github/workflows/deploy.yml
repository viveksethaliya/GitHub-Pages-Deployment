name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'index.html'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ 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 }}
          publish_dir: ./ # Or the directory containing your website files if not at the root